# Stdlib Numeric Library

`lib/numeric/` contains numeric data structures and algorithms that are larger
than the small helpers in `lib/core/`.

Use this directory for reusable numeric domains such as arbitrary precision
integers or decimals. Keep parser/formatting policy in `lib/text` unless the
format is inseparable from the numeric type.
