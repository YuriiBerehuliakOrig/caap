; tests/bad_import_shape.caap — malformed directive: import without an
; alias. Loading must fail with a LOCATED usage error, not a cryptic deep one.
(module stdlib.fixtures.bad_import_shape)
(import stdlib.lib.core.math)
(bind x (lambda () 1))
(export x)
